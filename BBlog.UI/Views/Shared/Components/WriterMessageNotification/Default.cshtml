@model List<Message>

<li class="nav-item dropdown">
    <a class="nav-link count-indicator dropdown-toggle" id="messageDropdown" href="#" data-toggle="dropdown" aria-expanded="false">
        <i class="mdi mdi-email-outline"></i>
        @if (Model.Count > 0)
        {
            <span class="count-symbol bg-warning"></span>
        }
    </a>
    <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="messageDropdown">
        <h6 class="p-3 mb-0">Messages</h6>
        @foreach (var item in Model)
        {
            <div class="dropdown-divider"></div>
            <a class="dropdown-item preview-item">
                <div class="preview-thumbnail">
                    <img src="~/themes/BBlogWriterTheme/assets/images/faces/face4.jpg" alt="image" class="profile-pic">
                </div>
                <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
                    <h6 class="preview-subject ellipsis mb-1 font-weight-normal">@item.Sender send you a message</h6>
                    <p class="text-gray mb-0">
                        @if (DateTime.Now.Subtract(item.Date).Days > 0)
                        {
                            @DateTime.Now.Subtract(item.Date).Days <span>  days and </span> @DateTime.Now.Subtract(item.Date).Hours <span> hours ago</span>
                        }
                        else if (DateTime.Now.Subtract(item.Date).Hours > 0)
                        {
                            @DateTime.Now.Subtract(item.Date).Hours <span> hours and </span> @DateTime.Now.Subtract(item.Date).Minutes <span> minutes ago</span>
                        }
                        else if (DateTime.Now.Subtract(item.Date).Minutes > 0)
                        {
                            @DateTime.Now.Subtract(item.Date).Minutes <span> minutes ago</span>
                        }
                    </p>
                </div>
            </a>
        }
        <h6 class="p-3 mb-0 text-center">4 New Messages</h6>
    </div>
</li>